name: CI - Build and Test

on:
  push:
    branches: [master, main, develop,feature/*]
  pull_request:
    branches: [master, main, feature/*]

env:
  PYTHON_VERSION: "3.12"

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          # Install dependencies for Playwright
          sudo apt-get install -y libasound2t64
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint with flake8
        run: |
          pip install flake8
          # Stop the build if there are Python syntax errors or undefined names
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude=.env,venv,env,.venv,.git,__pycache__,.pytest_cache,htmlcov,*.egg-info,build,dist
          # Exit-zero treats all errors as warnings
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics --exclude=.env,venv,env,.venv,.git,__pycache__,.pytest_cache,htmlcov,*.egg-info,build,dist
        # continue-on-error: true

      - name: Run unit tests
        run: |
          pytest tests/test_blood_pressure.py tests/test_app.py tests/test_health_tips.py -v --tb=short

      - name: Run BDD tests
        run: |
          pytest tests/bdd/ -v --tb=short

      - name: Generate coverage report
        run: |
          pytest --cov=. --cov-report=xml --cov-report=html --cov-report=term-missing

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-${{ matrix.python-version }}

  lint:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install flake8 black pylint bandit safety

      - name: Run Black
        run: |
          black --check .

      - name: Run Pylint
        run: |
          pylint **/*.py || exit 0

  security:
    name: Security Scanning
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install bandit safety

      - name: Run Bandit security linter
        run: |
          bandit -r . -f json -o bandit-report.json -x ./.env,./venv,./env,./.venv,./.git,./__pycache__,./.pytest_cache,./htmlcov,./build,./dist,./tests,./infrastructure 2>/dev/null || true
          bandit -r . -ll -x ./.env,./venv,./env,./.venv,./.git,./__pycache__,./.pytest_cache,./htmlcov,./build,./dist,./tests,./infrastructure

      - name: Check for known vulnerabilities with Safety
        run: |
          safety check --json || true
        # safety check

      - name: Upload Bandit report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-security-report
          path: bandit-report.json
        if: always()
